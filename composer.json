{
    "name": "icatch/catch",
    "description": "Catch B2B deployment helpers",
    "type": "project",
    "require": {},
    "scripts": {
        "release": "TAG=${RELEASE_TAG:-7.4-$(date +%Y%m%d)}; docker build -t harbor.icatch.ro/ice/icatch-b2b:${TAG} . && docker push harbor.icatch.ro/ice/icatch-b2b:${TAG}"
    }
}
